import { WeakRoleFilterDirective } from './weak-role-filter.directive';

describe('WeakRoleFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new WeakRoleFilterDirective();
    expect(directive).toBeTruthy();
  });
});
